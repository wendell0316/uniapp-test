const doNothing = () => {
  return;
};

export {
  doNothing as request,
  doNothing as showToast,
  doNothing as getStorageItem,
  doNothing as setStorageItem,
  doNothing as removeStorageItem,
  doNothing as subscribeEvent,
  doNothing as unsubscribeEvent,
  doNothing as publishEvent,
  doNothing as on,
  doNothing as open,
  doNothing as globalConfig,
};
